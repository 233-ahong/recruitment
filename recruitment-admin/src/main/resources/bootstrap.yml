# Tomcat
server:
  port: 9100

# Spring
spring:
  application:
    name: recruitment-admin
  cloud:
    nacos:
      discovery:
        server-addr: http://ahong.ltd:8848
      config:
        server-addr: http://ahong.ltd:8848
        file-extension: yml
        group: DEFAULT_GROUP